export enum DecoratorMetadata {
  Roles = 'roles',
}

export enum StrategyType {
  RefreshTokenJwt = 'refreshTokenJwt',
  Jwt = 'jwt',
  Local = 'local',
}

export enum Role {
  User = 'user',
  Admin = 'admin',
}
